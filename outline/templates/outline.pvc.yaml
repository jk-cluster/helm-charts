apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.outline.storage.size }}
  {{- if .Values.outline.storage.storageClass }}
  storageClassName: {{ .Values.outline.storage.storageClass }}
  {{- end }}