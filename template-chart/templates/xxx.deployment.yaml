apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-deployment
  labels:
    app: {{ .Release.Name }}-{{ .Chart.Name }}-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-{{ .Chart.Name }}-deployment
  template:
    metadata:
      name: {{ .Release.Name }}-{{ .Chart.Name }}-deployment
      labels:
        app: {{ .Release.Name }}-{{ .Chart.Name }}-deployment
    spec:
      automountServiceAccountToken: false
      containers:
        - name: {{ .Release.Name }}-{{ .Chart.Name }}-deployment
          image: "registry/image:{{ .Chart.AppVersion }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              protocol: TCP
              name: http
          # TODO: Add liveness and readiness probes
          resources:
            requests:
              memory: "{{ .Values.xxx.resources.requests.memory }}"
              cpu: "{{ .Values.xxx.resources.requests.cpu }}"
              ephemeral-storage: "{{ .Values.xxx.resources.requests.ephemeralStorage }}"
            limits:
              memory: "{{ .Values.xxx.resources.limits.memory }}"
              cpu: "{{ .Values.xxx.resources.limits.cpu }}"
              ephemeral-storage: "{{ .Values.xxx.resources.limits.ephemeralStorage }}"
      restartPolicy: Always
