apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
spec:
  selector:
    app: {{ .Release.Name }}-deployment
  ports:
    - nodePort: {{ .Values.server.outwardPort }}
      port: 25565
      targetPort: 25565
      name: proxy-port
  type: NodePort
  